| STT | Đề | Dịch | Các lựa chọn | Đáp án đúng | Giải thích |
|---|---|---|---|---|---|
| 1 | What is the syntax to look up the fullname attribute in an object stored in the variable colleen? | Cú pháp để tra cứu thuộc tính fullname trong một đối tượng được lưu trữ trong biến colleen là gì? | A. colleen->fullname<br>B. colleen::fullname<br>C. colleen.fullname<br>D. colleen['fullname'] | C | Trong Python, ta sử dụng dot notation (.) để truy cập thuộc tính của đối tượng. |
| 2 | Object and class attributes are accessed using ___ notation in Python. | Thuộc tính của đối tượng và lớp được truy cập bằng ký hiệu ___ trong Python. | A. Dot notation<br>B. Arrrrow notation<br>C. Plus notation<br>D. And notation | A | Thuộc tính của lớp và đối tượng luôn được truy cập thông qua dot notation. |
| 3 | Which of the following code uses the inheritance feature of Python? | Mã nào sau đây sử dụng tính năng kế thừa của Python? | A. Class Foo: Pass<br>B. Class Foo(object): passclass Hoo(object): pass<br>C. Class Foo: pass class Hoo(Foo): pass<br>D. None of the others. | C | Kế thừa được định nghĩa bằng cách đưa lớp cha vào trong ngoặc đơn khi khai báo lớp con. |
| 4 | What is the output of the following code?<br>class Foo:<br>&nbsp;&nbsp;def printLine(self, line='Python'):<br>&nbsp;&nbsp;&nbsp;&nbsp;print(line)<br>o1 = Foo()<br>o1.printLine('Java') | Kết quả đầu ra của đoạn mã sau là gì? | A. Python<br>B. Line<br>C. Java<br>D. Java Python | C | Tham số truyền vào ('Java') sẽ ghi đè giá trị mặc định ('Python'). |
| 5 | Which came first, the instance or the class? | Cái nào có trước, thực thể (instance) hay lớp (class)? | A. Class<br>B. Function<br>C. Instance<br>D. Method | A | Class là bản thiết kế (blueprint), phải có class trước khi tạo ra instance. |
| 6 | Which of the following is the correct way to define an initializer method? | Cách nào sau đây là đúng để định nghĩa một phương thức khởi tạo? | A. def \_\_init\_\_(title, author):<br>B. def \_\_init\_\_(self, title, author):<br>C. def \_\_init\_\_():<br>D. \_\_init\_\_(self, title, author): | B | Phương thức khởi tạo (\_\_init\_\_) cần tham số self để tham chiếu đến đối tượng đang được tạo. |
| 7 | class Person:<br>&nbsp;&nbsp;def \_\_init\_\_(self, name, age):<br>&nbsp;&nbsp;&nbsp;&nbsp;self.name = name<br>&nbsp;&nbsp;&nbsp;&nbsp;self.age = age<br>The correct way to instantiate the above Person class is: | Cách đúng để khởi tạo lớp Person ở trên là: | A. Person ("Rufus", 3)<br>B. Person ()<br>C. Person.init("Rufus", 3)<br>D. Person.create("Rufus", 3) | A | Để khởi tạo đối tượng, ta gọi tên lớp kèm theo các đối số cần thiết cho hàm \_\_init\_\_. |
| 8 | What is "self" typically used for in a Python method within a class? | "self" thường được sử dụng để làm gì trong một phương thức Python bên trong một lớp? | A. To terminate a loop<br>B. To set the residual value in an expression where the method is used<br>C. The number of parameters to the method<br>D. To refer to the instance in which the method is being called | D | 'self' là biến đại diện cho chính thực thể (instance) đang gọi phương thức đó. |
| 9 | What does this Python statement do if PartyAnimal is a class? zap = PartyAnimal() | Câu lệnh Python này làm gì nếu PartyAnimal là một lớp? zap = PartyAnimal() | A. Clear out all the data in the PartyAnimal variable and put the old values for the data in zap<br>B. Subtract the value of the zap variable from the value in the PartyAnimal variable and put the difference in zap<br>C. Use the PartyAnimal template to make a new object and assign it to zap<br>D. Copy the value from the PartyAnimal variable to the variable zap | C | Cú pháp `ClassName()` sẽ tạo ra một instance mới của lớp đó. |
| 10 | What does the following Python Program print out?<br>str1 = "Hello"<br>str2 = 'there'<br>bob = str1 + str2<br>print(bob) | Chương trình Python sau đây in ra cái gì? | A. Hello<br>B. 0<br>C. Hellothere<br>D. Hello there | C | Toán tử '+' dùng cho string sẽ thực hiện phép nối chuỗi (concatenation) mà không tự động thêm dấu cách. |
| 11 | For the following list, how would you print out 'Sally'?<br>friends = [ 'Joseph', 'Glenn', 'Sally' ] | Đối với danh sách sau, làm thế nào bạn có thể in ra 'Sally'? | A. print friends[3]<br>B. print(friends['Sally'])<br>C. print(friends[2])<br>D. print(friends[2:1]) | C | List trong Python đánh chỉ số từ 0. 'Sally' ở vị trí thứ 3 nên index là 2. |
| 12 | For the following code:<br>astr = 'Hello Bob'<br>istr = 0<br>try:<br>&nbsp;&nbsp;istr = int(astr)<br>except:<br>&nbsp;&nbsp;istr = -1<br>What will the value be for istr variable after this code executes? | Đối với đoạn mã sau, giá trị của biến istr sau khi mã này thực thi sẽ là bao nhiêu? | A. -1<br>B. It will be the 'Not a number' value (i.e. NaN)<br>C. It will be a random number depending on the operating system the program runs on<br>D. false | A | Vì 'Hello Bob' không thể chuyển sang số nguyên, khối `except` sẽ thực thi và gán `istr = -1`. |
| 13 | Which of the following would separate a string input_string on the first 2 occurences of the letter "e"? | Cái nào sau đây sẽ tách chuỗi input_string tại 2 lần xuất hiện đầu tiên của chữ cái "e"? | A. 'e'.split(input_string, 1)<br>B. 'e'.split(input_string, 2)<br>C. 'e'.split(input_string, maxsplit=2)<br>D. input_string.split('e', maxsplit=2) | D | Phương thức `split` của string nhận dấu phân cách làm đối số thứ nhất và `maxsplit` để giới hạn số lần tách. |
| 14 | Which method can be used to replace parts of a string? | Phương thức nào có thể được sử dụng để thay thế các phần của một chuỗi? | A. replace()<br>B. replaceString()<br>C. repl()<br>D. rep() | A | `replace()` là phương thức chuẩn của string để thay thế chuỗi con. |
| 15 | What is a correct syntax to return the first character in a string? | Cú pháp đúng để trả về ký tự đầu tiên trong một chuỗi là gì? | A. x = "Hello".sub(0, 1)<br>B. x = "Hello"[1]<br>C. x = "Hello"[0]<br>D. x = sub("Hello", 0, 1) | C | Truy cập ký tự đầu tiên của chuỗi bằng index 0. |
| 16 | What is output for:<br>b = [11,13,15,17,19,21]<br>print(b[::2]) | Kết quả đầu ra cho đoạn mã sau là gì? | A. [19,21]<br>B. [11,15]<br>C. [11,15,19]<br>D. [13,17,21] | C | Slicing `[::2]` lấy các phần tử từ đầu đến cuối với bước nhảy là 2 (11, 15, 19). |
| 17 | What will the following Python code print out?<br>friends = [ 'Joseph', 'Glenn', 'Sally' ]<br>friends.sort()<br>print friends[1] | Mã Python sau đây sẽ in ra cái gì? | A. Glenn<br>B. Joseph<br>C. Sally<br>D. Friends | B | Sau khi `sort()`, thứ tự là ['Glenn', 'Joseph', 'Sally']. `friends[1]` là 'Joseph'. |
| 18 | Which of these collections defines a DICTIONARY? | Bộ sưu tập nào trong số này định nghĩa một DICTIONARY? | A. ("apple", "banana", "cherry")<br>B. {"name": "apple", "color": "green"}<br>C. {"apple", "banana", "cherry"}<br>D. ["apple", "banana", "cherry"] | B | Dictionary được định nghĩa bằng cặp `{key: value}`. |
| 19 | What is the purpose of the newline character in text files? | Mục đích của ký tự dòng mới (newline) trong các tệp văn bản là gì? | A. It adds a new network connection to retrieve files from the network<br>B. It indicates the end of one line of text and the beginning of another line of text<br>C. It enables random movement throughout the file<br>D. It allows us to open more than one files and read them in a synchronized manner | B | Ký tự newline (\n) dùng để báo kết thúc một dòng và bắt đầu dòng mới. |
| 20 | Which of the following command is used to open a file "c:\temp.txt" in write-mode only? | Lệnh nào sau đây được sử dụng để mở một tệp "c:\temp.txt" chỉ ở chế độ ghi? | A. outfile = open("c:\temp.txt", "w")<br>B. outfile = open("c:\\\\temp.txt", "w")<br>C. outfile = open(file = "c:\temp.txt", "w+")<br>D. outfile = open(file = "c:\\\\temp.txt", "w+") | B | Cần dùng `\\` để escape ký tự đặc biệt trong đường dẫn file và chế độ 'w' cho write-only. |
| 21 | What do we use the second parameter of the open() call to indicate | Chúng ta sử dụng tham số thứ hai của lệnh gọi open() để chỉ ra điều gì? | A. How large we expect the file to be<br>B. The list of folders to be searched to find the file we want to open<br>C. Whether we want to read data from the file or write data to the file<br>D. What disk drive the file is stored on | C | Tham số thứ hai của hàm `open()` là mode, xác định mục đích mở file (đọc, ghi, v.v.). |
| 22 | How do you insert something on a new line in a file? | Làm thế nào để bạn chèn một cái gì đó vào một dòng mới trong một tệp? | A. write.newline('x')<br>B. \n<br>C. You cannot do this<br>D. type the content on the line below | B | Ký tự `\n` đại diện cho một dòng mới. |
| 23 | If we open a file as follows:<br>xfile = open('mbox.txt')<br>What statement would we use to read the file one line at a time? | Nếu chúng ta mở một tệp như sau, câu lệnh nào chúng ta sẽ sử dụng để đọc tệp từng dòng một? | A. for line in xfile:<br>B. while ( getline (xfile,line) ) {<br>C. READ xfile INTO LINE<br>D. READ (xfile,*,END=10) line | A | Trong Python, ta có thể lặp trực tiếp qua đối tượng file handler để đọc từng dòng. |
| 24 | The following code sequence fails with a traceback when the user enters a file that does not exist. How would you avoid the traceback and make it so you could print out your own error message when a bad file name was entered?<br>fname = raw_input('Enter the file name: ')<br>fhand = open(fname) | Chuỗi mã sau đây thất bại với một traceback khi người dùng nhập một tệp không tồn tại. Làm thế nào để bạn tránh traceback và in ra thông báo lỗi của riêng mình? | A. try / except<br>B. signal handlers<br>C. try / catch / finally<br>D. on error resume next | A | `try/except` là cấu trúc để xử lý ngoại lệ (exceptions) trong Python. |
| 25 | Which of the parts of a computer actually executes the program instructions? | Bộ phận nào của máy tính thực sự thực thi các chỉ thị chương trình? | A. RAM<br>B. CPU<br>C. Input/Output Devices<br>D. Secondary Memory | B | CPU (Central Processing Unit) là bộ phận thực thi các chỉ thị của chương trình. |
| 26 | You want to create a bytes object consisting of five null (0x00) bytes. All of the following will work except one. Select the one that doesn't work: | Bạn muốn tạo một đối tượng bytes bao gồm năm byte null (0x00). Tất cả những điều sau đây sẽ hoạt động ngoại trừ một cái. Chọn cái không hoạt động: | A. bytes(5)<br>B. bytes(0, 0, 0, 0, 0)<br>C. bytes('\x00\x00\x00\x00\x00', 'utf-8')<br>D. bytes([0] * 5) | B | Hàm `bytes()` không nhận danh sách các số nguyên rời rạc làm đối số. Option A, C, D đều hợp lệ. |
| 27 | What is the output from this print() function call:<br>print(<br>&nbsp;&nbsp;'$100 $200 $300'.count('$'),<br>&nbsp;&nbsp;'$100 $200 $300'.count('$', 5, 10),<br>&nbsp;&nbsp;'$100 $200 $300'.count('$', 5)<br>) | Kết quả đầu ra từ lệnh gọi hàm print() này là gì? | A. 3 1 2<br>B. 3 1 1<br>C. 3 2 1<br>D. 3 1 0 | A | Lần 1 đếm tất cả (3). Lần 2 đếm từ index 5 đến 10 (chỉ có $ ở index 5). Lần 3 đếm từ index 5 đến hết (có $ ở index 5 và 10). |
| 28 | What will the following program print out:<br>>>> x = 15<br>>>> x = x + 10<br>>>> print(x) | Chương trình sau đây sẽ in ra cái gì? | A. 25<br>B. 5<br>C. 15<br>D. "print x" | A | Thực hiện phép cộng 15 + 10 và gán lại cho x. |
| 29 | What are the names of the two types of mode that are used in Python? | Tên của hai loại chế độ được sử dụng trong Python là gì? | A. Interactive mode & Script mode<br>B. Interactive mode & Scratch mode<br>C. Interactive mode & Imperial mode<br>D. Imperial mode & Script mode | A | Python có hai chế độ hoạt động chính là Interactive (tương tác trực tiếp) và Script (chạy từ file). |
| 30 | What is the proper way to say "good-bye" to Python? | Cách đúng nhất để nói "tạm biệt" với Python là gì? | A. quit()<br>B. stop()<br>C. exit()<br>D. end() | A | `quit()` là lệnh thông dụng để thoát khỏi trình thông dịch Python. |
| 31 | Consider this assignment statement:<br>a, b, c = (1, 2, 3, 4, 5, 6, 7, 8, 9)[1::3]<br>Following execution of this statement, what is the value of b: | Xem xét câu lệnh gán sau, giá trị của b là bao nhiêu? | A. 2<br>B. 4<br>C. 5<br>D. 6 | C | Slicing `(1,2,3,4,5,6,7,8,9)[1::3]` trả về `(2, 5, 8)`. Do đó `b` nhận giá trị 5 thông qua tuple unpacking. |
| 32 | Find the output of the code given below?<br>content = (21, 26, 37, 94)<br>content.append( (75, 62, 79) )<br>print(len(content)) | Tìm kết quả đầu ra của đoạn mã dưới đây? | A. 7<br>B. 2<br>C. 1<br>D. Error | D | Tuple là kiểu dữ liệu bất biến (immutable), không có phương thức `append()`. |
| 33 | You have the following dictionary definition:<br>d = {'foo': 100, 'bar': 200, 'baz': 300}<br>What method call will delete the entry whose value is 200? | Bạn có định nghĩa từ điển sau, lệnh gọi phương thức nào sẽ xóa mục có giá trị là 200? | A. d.pop("bar")<br>B. d.remove("bar")<br>C. d.remove("200")<br>D. d.pop("200") | A | `pop()` trong dictionary nhận tham số là key ('bar'). Không có phương thức `remove()` cho dictionary. |
| 34 | What is the result of this statement?<br>print(ord('foo')) | Kết quả của câu lệnh này là gì? | A. 102<br>B. It raises an exception<br>C. 102 111 111<br>D. 324 | B | Hàm `ord()` chỉ nhận đối số là một ký tự duy nhất. |
| 35 | What does the \_\_init\_\_() the function do in Python? | Hàm \_\_init\_\_() làm gì trong Python? | A. Initializes the class for use.<br>B. This function is called when a new object is instantiated.<br>C. Initializes all the data attributes to zero when called.<br>D. None of the above. | B | `__init__` là phương thức khởi tạo, tự động được gọi khi một thực thể mới của lớp được tạo ra. |
| 36 | What does the Python dir() function show when we pass an object into it as a parameter? | Hàm dir() của Python hiển thị những gì khi chúng ta truyền một đối tượng vào đó như một tham số? | A. It shows the parent class<br>B. It shows the number of parameters to the constructor<br>C. It shows the methods and attributes of the object<br>D. It shows the type of the object | C | Hàm `dir()` trả về danh sách các thuộc tính và phương thức hợp lệ của đối tượng được truyền vào. |
| 37 | Which of the following reserved keyword is used to end a function, return generator? | Từ khóa dự phòng nào sau đây được sử dụng để kết thúc một hàm và trả về một generator? | A. break<br>B. switch<br>C. Return<br>D. yield | D | Từ khóa `yield` được sử dụng trong hàm để trả về một generator mà không làm mất trạng thái của hàm. |
| 38 | Find the output of the given Python program:<br>a = 25<br>if a < 15:<br>&nbsp;&nbsp;print("Hi")<br>if a <= 30:<br>&nbsp;&nbsp;print("Hello")<br>else:<br>&nbsp;&nbsp;print("Know Program") | Tìm kết quả đầu ra của chương trình Python đã cho: | A. Hi<br>B. Hello<br>C. Hi Hello<br>D. Hi Know Program | B | `a < 15` là sai, `a <= 30` là đúng, nên chương trình chỉ in ra "Hello". |
| 39 | Which of these collections defines a LIST? | Bộ sưu tập nào trong số này định nghĩa một LIST? | A. {"name": "apple", "color": "green"}<br>B. ["apple", "banana", "cherry"]<br>C. {"apple", "banana", "cherry"}<br>D. ("apple", "banana", "cherry") | B | List trong Python được bao quanh bởi dấu ngoặc vuông `[]`. |
| 40 | Which of the following slicing operations will produce the list [12, 3]?<br>t = [9, 41, 12, 3, 74, 15] | Hoạt động slicing nào sau đây sẽ tạo ra danh sách [12, 3]? | A. t[1:3]<br>B. t[2:2]<br>C. t[:]<br>D. t[2:4] | D | Slicing `t[2:4]` lấy các phần tử từ index 2 đến index 3 (12, 3). |
| 41 | What is a correct syntax to return the third character in a string? | Cú pháp đúng để trả về ký tự thứ ba trong một chuỗi là gì? | A. x = "Hello"[2]<br>B. x = sub("Hello",0,2)<br>C. x = "Hello".sub(0,2)<br>D. x = sub "Hello" | A | Truy cập ký tự thứ ba của chuỗi bằng index 2. |
| 42 | Which of these words is a reserved word in Python ? | Từ nào trong số này là một từ khóa dự phòng trong Python? | A. while<br>B. payroll<br>C. names<br>D. pizza | A | `while` là từ khóa dự phòng (reserved word) dùng cho vòng lặp trong Python. |
| 43 | What is the value of the following expression? 42 % 10 | Giá trị của biểu thức sau là bao nhiêu? 42 % 10 | A. 2<br>B. 420<br>C. 4210<br>D. 42 | A | Toán tử `%` trả về số dư của phép chia. 42 chia 10 dư 2. |
| 44 | Assume x and y are assigned as follows:<br>x = 5<br>y = -5<br>What is the effect of this statement:<br>x, y = (y, x)[::-1] | Giả sử x và y được gán như sau, hiệu ứng của câu lệnh này là gì? | A. The values of x and y are unchanged<br>B. Both x and y are -5<br>C. Both x and y are 5<br>D. The values of x and y are swapped | A | `(y, x)[::-1]` đảo ngược tuple `(y, x)` thành `(x, y)`. Do đó `x, y = x, y` không làm thay đổi giá trị. |
| 45 | What will be the value of x after the following statement executes:<br>x = 2 * 3 - 8 / 4 % 2 | Giá trị của x sau khi câu lệnh sau thực thi sẽ là bao nhiêu? | A. 1<br>B. 2<br>C. 4<br>D. 6 | D | Thứ tự ưu tiên: nhân/chia/chia lấy dư trước, từ trái sang phải. 2*3=6, 8/4=2, 2%2=0. Cuối cùng là 6 - 0 = 6. |
| 46 | Which of the following elements of a mathematical expression in Python is evaluated first? | Thành phần nào của biểu thức toán học trong Python được đánh giá đầu tiên? | A. Addition +<br>B. Multiplication *<br>C. Subtraction -<br>D. Parentheses () | D | Trong biểu thức toán học, các phần trong ngoặc đơn luôn được ưu tiên xử lý trước. |
| 47 | In the following code, x = 9 What is x? | Trong đoạn mã sau, x = 9, x là gì? | A. A variable<br>B. A constant<br>C. A parameter<br>D. A pointer | A | `x` là một biến (variable) dùng để lưu trữ giá trị. |
| 48 | You look at the following text:<br>if x == 6 :<br>&nbsp;&nbsp;print('Is 6')<br>&nbsp;&nbsp;print('Is Still 6')<br>&nbsp;&nbsp;print('Third 6')<br>It looks perfect but Python is giving you an 'Indentation Error' on the second print statement. What is the most likely reason? | Bạn nhìn vào đoạn văn bản sau, lý do có khả năng nhất cho lỗi 'Indentation Error' là gì? | A. In order to make humans feel inadequate...<br>B. Python has reached its limit...<br>C. Python thinks 'Still' is a mis-spelled word...<br>D. You have mixed tabs and spaces in the file | D | Một trong những nguyên nhân phổ biến gây lỗi Indentation Error là do trộn lẫn giữa phím Tab và dấu cách (Spaces). |
| 49 | What is true about the following code segment:<br>if x == 5 :<br>&nbsp;&nbsp;print('Is 5')<br>&nbsp;&nbsp;print('Is Still 5')<br>&nbsp;&nbsp;print('Third 5') | Điều gì đúng về phân đoạn mã sau: | A. Depending on the value of x, either all three of the print statements will execute or none of the statements will execute<br>B. The string 'Is 5' will always print out regardless of the value for x.<br>C. The string 'Is 5' will never print out regardless of the value for x.<br>D. Only two of the three print statements will print out if the value of x is less than zero. | A | Cả ba câu lệnh đều được thụt lề dưới cùng một khối `if`, nên chúng sẽ cùng được thực thi hoặc cùng không thực thi tùy thuộc vào điều kiện `x == 5`. |
| 50 | What is the Python reserved word that we use in two-way if tests to indicate the block of code that is to be executed if the logical test is false? | Từ khóa dự phòng Python nào chúng ta sử dụng trong các bài kiểm tra if hai chiều để chỉ ra khối mã được thực thi nếu kiểm tra logic là sai? | A. break<br>B. switch<br>C. else<br>D. toggle | C | `else` là từ khóa dùng để thực thi khối mã khi điều kiện `if` là sai. |
